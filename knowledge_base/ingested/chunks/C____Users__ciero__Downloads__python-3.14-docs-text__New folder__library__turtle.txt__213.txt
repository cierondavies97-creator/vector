>>> screen.tracer(8, 25)
      >>> dist = 2
      >>> for i in range(200):
      ...     fd(dist)
      ...     rt(90)
      ...     dist += 2
turtle.update()
   Perform a TurtleScreen update. To be used when tracer is turned