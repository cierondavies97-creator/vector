acquired a lock, subsequent attempts to acquire it block, until it
   is released; any thread may release it.
   Changed in version 3.13: "Lock" is now a class. In earlier Pythons,
   "Lock" was a factory function which returned an instance of the