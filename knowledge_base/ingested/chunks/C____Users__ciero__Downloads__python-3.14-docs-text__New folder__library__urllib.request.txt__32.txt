letter no longer cause an "OSError" exception to be raised on
   Windows.
   Changed in version 3.14: The URL authority is discarded if it
   matches the local hostname. Otherwise, if the authority isn't empty