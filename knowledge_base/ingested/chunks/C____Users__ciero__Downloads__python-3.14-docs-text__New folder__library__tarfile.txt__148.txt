used.
     Changed in version 3.14: The default filter is set to "data".
     Previously, the default was equivalent to "fully_trusted".
* A callable which will be called for each extracted member with a