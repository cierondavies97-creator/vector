once as long as the sort has not started. Previously this raised
      "ValueError".
   is_active()
      Returns "True" if more progress can be made and "False"
      otherwise. Progress can be made if cycles do not block the