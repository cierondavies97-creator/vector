space). The original string is returned if *width* is less than or
   equal to "len(s)".
   For example:
      >>> 'Python'.ljust(10)
      'Python    '
      >>> 'Python'.ljust(10, '.')
      'Python....'