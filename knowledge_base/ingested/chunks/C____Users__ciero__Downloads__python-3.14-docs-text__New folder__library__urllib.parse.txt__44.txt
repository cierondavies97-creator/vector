has been changed to allow only a single separator key, with "&" as
   the default separator.
   Deprecated since version 3.14: Accepting objects with false values
   (like "0" and "[]") except empty strings and byte-like objects and